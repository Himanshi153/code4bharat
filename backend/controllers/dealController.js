const Product = require('../models/product');

const getFlashDeals = async (req, res) => {
  // implementation
};

const reserveDeal = async (req, res) => {
  // implementation
};

module.exports = {
  getFlashDeals,
  reserveDeal
};
